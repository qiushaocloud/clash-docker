stages:
  - sync-code

sync-code:
  stage: sync-code
  only:
    - master
  script:
    - cat .git/config
    - git checkout master
    - sh push-remote-git-repos.sh
  tags:
    - qiushaocloud-runner
